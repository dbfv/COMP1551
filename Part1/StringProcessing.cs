namespace Part1 {
    internal class StringProcessing {
        public StringProcessing(string S, int N) {
            //string errorMessage = "";
            //if (inputString.Length > 40) { //check if inputString.length > 40 
            //    //if so then tell user that inputString max length = 40 
            //    errorMessage += "You have exceeded the maximum number of characters! \n";
            //}
            //else {
            //    errorMessage = errorMessage.Replace("You have exceeded the maximum number of characters! \n", "");
            //}

            //if (N < -25 || N > 25) { //check if N is not in [-25..25]
            //    //if so, tell user to re-enter N
            //    errorMessage += "N must be within from -25 to 25! \n ";
            //}
            //else {
            //    errorMessage = errorMessage.Replace("N must be within from -25 to 25! \n ", "");
            //}

            ////if inputString.length < 40 and N is in [-25..25] then: 
            //bool IsCapitalLetter = Regex.IsMatch(inputString, @"^[A-Z]+$"); //check if string only contain uppcase letters
            //while (!IsCapitalLetter) { //if not 
            //    //ask user to re-enter the string 
            //}



        }
    }
}
